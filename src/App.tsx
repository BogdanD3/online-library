import React, { Fragment } from "react";
import Layout from "./Components/Layout/Layout";

const App: React.FC = () => {
  return (
    <Fragment>
      <Layout />
    </Fragment>
  );
};

export default App;
